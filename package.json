{
  "scripts": {
    "server": "npm run dev --prefix server",
    "build": "npm install --prefix server"
  },
  "dependencies": {
    "brew": "^0.0.8"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}